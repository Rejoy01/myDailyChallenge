function Filter_lis(l){
    
    let result = []

    l.map((item)=>{
        
        if(typeof item != "string"){
            val.push(item)
        }
       
    })
    return result
}
console.log(Filter_lis([1, "2", 3,"4"])) 